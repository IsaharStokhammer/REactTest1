export interface Mission{
    name: string;
    status: string;
    priority: string;
    description: string;
}

const exsampleMission = {
    name: "Mission 1",
    status: "In progress",
    priority: "Medium",
    description: "Mission 1 description",
}